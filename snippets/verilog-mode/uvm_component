# -*- mode: snippet -*-
# key: uc
# --

class ${1:class_name} extends ${2:uvm_component};
    `uvm_component_utils($1)

    $0

    function new(string name="${3:$1}", uvm_component parent);
        super.new(name, parent);
    endfunction : new

endclass: $1
